[path]
# input for fwi code - fwi related intp file path
fwipath = /home/user/fwi3d
# source wavelet file
waveletfile = ${fwipath}/data/source_function.npy
# data file
datafile = ${fwipath}/data/observed_data.npy
# source file
srcfile = ${fwipath}/data/sources.npy
# receiver file
recfile = ${fwipath}/data/receivers.npy

# input directory for tdwi code
inpath = ${fwipath}/input
# output directory for tdfwi code
outpath = ${fwipath}/output

[dask]
# parallel environment used on a hpc system
pe = parallel_env_name
# number of nodes for each dask worker 
nnodes = 2
# number of dask workers, the total nodes required will be nnodes*nworkers
nworkers = 2
# number of threads for each dask worker, usually set to one to let the third-party code decide the number of threads, e.g. by using omp_num_threads
ph = 1
# directory for dask 
daskpath = ${path:fwipath}/dask


[FWI]
# wavter layers where the velocity values are fixed as the true values
water_layer = 1
# true velocity value in the layer with fixed velocity value
vel_fixed = 1500.
# grid points in z direction
nz = 63
# grid points in y direction
ny = 101
# grid points in x direction
nx = 101
# grid spacing in z direction
dz = 50
# grid spacing in y direction
dy = 50
# grid spacing in x direction
dx = 50
# batch size for number of shots
shot_batch = 36
